import 'package:flutter/material.dart';

class AnalyticTab extends StatelessWidget {
  const AnalyticTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Halaman Analytic', style: TextStyle(fontSize: 24)),
    );
  }
}